import { Card } from "antd";
import styled from "styled-components";

export const StyledCard = styled(Card)`
  margin: auto;
`;

export const StyledMeta = styled(Card.Meta)`
  height: 80px;
  overflow: hidden;
`;
